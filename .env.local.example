# Fantasy Sports Platform - Environment Variables
# Copy this file to .env.local and fill in the values

# =============================================================================
# ENVIRONMENT IDENTIFICATION
# =============================================================================
# Set to 'production', 'sandbox', or 'development'
# - production: Real 2026 season, crons enabled, no dev badges
# - sandbox: Test environment with 2025 data, crons disabled, shows dev badge
# - development: Local development, crons disabled, shows dev badge
NEXT_PUBLIC_ENVIRONMENT=sandbox

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project: Settings > API

# Public URL (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous/public key (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (KEEP SECRET - server-side only)
# Used for admin operations like cron jobs, sync, and transactions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# SECURITY / AUTHENTICATION
# =============================================================================
# Secret for authenticating cron job requests from Vercel
# Generate with: openssl rand -hex 32
# Set this in Vercel dashboard for production
CRON_SECRET=your-cron-secret-here

# API key for sync endpoints (optional, has default fallback)
# Used by /api/sync/* and /api/points/calculate endpoints
SYNC_API_KEY=your-sync-api-key

# =============================================================================
# VERCEL ENVIRONMENT SETUP
# =============================================================================
# In Vercel dashboard (Settings > Environment Variables):
#
# FOR PRODUCTION (main branch):
#   NEXT_PUBLIC_ENVIRONMENT = production
#   NEXT_PUBLIC_SUPABASE_URL = [production supabase url]
#   NEXT_PUBLIC_SUPABASE_ANON_KEY = [production anon key]
#   SUPABASE_SERVICE_ROLE_KEY = [production service role key]
#   CRON_SECRET = [generate unique secret]
#   SYNC_API_KEY = [generate unique key]
#
# FOR PREVIEW (all other branches):
#   NEXT_PUBLIC_ENVIRONMENT = sandbox
#   NEXT_PUBLIC_SUPABASE_URL = [sandbox supabase url]
#   NEXT_PUBLIC_SUPABASE_ANON_KEY = [sandbox anon key]
#   SUPABASE_SERVICE_ROLE_KEY = [sandbox service role key]
#   CRON_SECRET = [different secret for sandbox]
#   SYNC_API_KEY = [different key for sandbox]
